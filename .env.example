# Zero-config push: install ntfy app and subscribe this topic
NTFY_SERVER=https://ntfy.sh
NTFY_TOPIC=realllllty_flight_check
NOTIFY_ENABLED=true

# mock (default) or real provider name
FLIGHT_PROVIDER=mock
# required when FLIGHT_PROVIDER != mock
FLIGHT_PROVIDER_ENDPOINT=
FLIGHT_PROVIDER_TOKEN=
